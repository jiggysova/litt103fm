
# LITT103.FM — Starter Build (Multi-Page)
This scaffold includes:
- `index.html` (Homepage)
- `listen.html` (Listen Live page)
- `/assets/css/style.css` (theme: volcano / lava)
- `/assets/js/main.js` (mobile nav + audio controls)

## Stream URL
Replace the empty `src` on `<audio id="live-audio">` in both pages with the real stream URL (e.g. Shoutcast/Icecast HLS/MP3).

## Extend
Duplicate the header/footer into new pages (schedule, events, merch, community, about) to expand rapidly.
